<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Despensa</title>

    <!-- Custom fonts for this template-->
    <link href="/perfil/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <link
        href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i"
        rel="stylesheet">

    <!-- Custom styles for this template-->
    <link href="/perfil/css/sb-admin-2.min.css" rel="stylesheet">

    {{!-- <!-- jQUERY -->
    <script src="js/jquery-3.5.0.js"></script>

    <script type="text/javascript">

        function agregarFila() {
            document.getElementById("listaCompra").insertRow(-1).innerHTML = `
                        <tr>
                          <td><input type="checkbox"></td>
                          <td>${cantidad.value}</td>
                          <td>${producto.value}</td>
                        </tr>`
        }

        window.addEventListener("load", function () {
            let producto = document.getElementById("producto")
            let cantidad = document.getElementById("cantidad")
            let listaCompra = document.getElementById("listaCompra")
            let enviar = document.getElementById("enviar")
            let eliminar = document.getElementById("eliminar")
            let todo = document.getElementById("todo")

            enviar.addEventListener("click", function () {
                if (producto.value != '' && cantidad.value != '') {
                    agregarFila()
                }
            })

            eliminar.addEventListener("click", function () {
                $('input:checked').not('.all').parents("tr").remove();
            })

            todo.addEventListener("click", function () {
                var $inputs = $('table').find('input');
                $inputs.prop('checked', this.checked);

            })

        })

    </script> --}}

</head>

<body id="page-top">

    <!-- Page Wrapper -->
    <div id="wrapper">

        {{!-- barra lateral --}}
        {{>lateral_lista}}
        <!-- Content Wrapper -->
        <div id="content-wrapper" class="d-flex flex-column">

            <!-- Main Content -->
            <div id="content">

                {{!-- navegacion --}}
                {{>navegacion}}

                <!-- Begin Page Content -->
                <div class="container-fluid">

                    <!-- Page Heading -->
                    <div class="d-sm-flex align-items-center justify-content-between mb-4">
                        <h1 class="h3 mb-0 text-gray-800">Lista de la compra</h1>
                    </div>


                    <form action="/producto-nuevo" method="POST">
                        <div class="form-group">
                            <label for="producto">Producto</label>
                            <input type="text" class="form-control" name="nombre" placeholder="Producto para añadir">
                        </div>
                        <div class="form-group">
                            <label for="cantidad">Cantidad</label>
                            <input type="number" class="form-control" name="cantidad" min="1"
                                placeholder="Cantidad para añadir">
                        </div>
                        <div class="form-group">
                            <label for="precio_unidad">Precio por unidad</label>
                            <input type="number" class="form-control" name="precio_unidad" min="1"
                                placeholder="Precio para añadir">
                        </div>
                        <button type="submit" class="btn btn-primary btn-block">Añadir</button>
                        <button id="eliminar" type="button" class="btn btn-danger">Eliminar seleccionados</button>
                    </form>

                    <br>

                    <div class="container">
                        <div class="row">

                            <div class="col-12">
                                <table class="table table-bordered" id="listaCompra">
                                    <thead>
                                        <tr>
                                            <th scope="col"><input type="checkbox" id="todo"
                                                    class="all">&nbsp;Seleccionar todo</th>
                                            <th scope="col">Producto</th>
                                            <th scope="col">Cantidad</th>
                                            <th scope="col">Precio por unidad</th>
                                            <th scope="col">Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {{#each lista}}
                                        <tr>
                                            <td></td>
                                            <td>{{nombre}}</td>
                                            <td>{{cantidad}}</td>
                                            <td>{{precio_unidad}}</td>
                                            <td></td>
                                        </tr>
                                        {{/each}}
                                    </tbody>
                                </table>
                            </div>

                        </div>
                    </div>
                </div>
</body>

</html>